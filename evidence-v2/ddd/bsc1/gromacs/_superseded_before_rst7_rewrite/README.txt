GROMACS auto-backups from the first pass, made before the rst7 was rewritten
from the tleap PDB (see RESULTS.md section 2).  Those runs read hydrogen
coordinates at full tleap precision while MOSAICS read the 3-decimal PDB, and
are superseded by sp.xvg / sp.log one directory up.  Kept rather than deleted
so the two passes can be compared.
